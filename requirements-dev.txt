# Development dependencies are declared in pyproject.toml under
# [project.optional-dependencies].dev, so there is one list to keep current.
# This file stays for anyone used to `pip install -r requirements-dev.txt`.

-e .[dev]
