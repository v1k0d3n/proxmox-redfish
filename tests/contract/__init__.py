"""Contract (characterization) tests.

These capture the Redfish responses of a *running* daemon so that a
refactor can be proven byte-identical against the behaviour that is
actually deployed, rather than against what the code appears to do.
"""
