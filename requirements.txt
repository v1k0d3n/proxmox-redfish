# Runtime dependencies only.
# Development and test tooling lives in requirements-dev.txt, or install
# the package with its extra:  pip install -e ".[dev]"

proxmoxer>=2.0.0
requests>=2.32.0
requests-toolbelt>=1.0.0
